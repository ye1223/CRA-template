{
   "compilerOptions": {
      "baseUrl": "src", // 项目的基本目录
      "paths": { // 路径别名。
         "@/*": [
            "*"
         ]
      },
      "target": "es5", // 指定编译后的代码目标为 ECMAScript 5
      "lib": [
         "dom",
         "dom.iterable",
         "esnext"
      ], // 包含的库文件，支持 DOM 操作的类型定义和最新的 ECMAScript 特性
      "allowJs": true, // 允许编译 JavaScript 文件
      "skipLibCheck": true, // 跳过库文件（*.d.ts）的类型检查
      "esModuleInterop": true, // 启用 ES6 模块的互操作性
      "allowSyntheticDefaultImports": true, // 允许从没有默认导出的模块中默认导入
      "strict": true, // 启用所有严格的类型检查选项
      "forceConsistentCasingInFileNames": true, // 强制文件名大小写一致性
      "noFallthroughCasesInSwitch": true, // 防止 switch 语句贯穿
      "module": "esnext", // 使用的模块系统，这里是 ES6+ 的模块系统
      "moduleResolution": "node", // 模块解析策略，Node 用于 CommonJS 和 Node 模块解析
      "resolveJsonModule": true, // 允许导入 JSON 模块
      "isolatedModules": true, // 每个文件作为单独的模块进行编译
      "noEmit": true, // 不生成编译后的文件
      "jsx": "react-jsx" // 在 JSX 代码中使用的 JSX 转换方式，这里是为了兼容 React
   },
   "include": [
      "src"
   ] // 指定要包括在编译中的文件夹，这里是项目的 src 目录
}